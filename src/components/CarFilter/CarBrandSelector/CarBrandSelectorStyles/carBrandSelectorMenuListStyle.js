import styled from "@emotion/styled";

const MenuListContainerStyleBase = styled.div`
          height: 244px;
          width: 190px;

          overflow: hidden;
          position: relative;
        `

const        menu-list = styled.`
          list-style: none;
          margin: 0;
          padding: 0;

          position: absolute;
          left: 0px;          
        `

const        menu-item = styled.`
          color: rgba(18, 20, 23, 0.2);
          
          transition: 500ms;
                &:hover 
          color: rgb(18, 20, 23);
        }`

const        menu-option-radio styled.`
          display: none;
        `;

export {  };
