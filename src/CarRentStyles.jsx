import { Global, css } from "@emotion/react";

import {} from "./";

const CarFilterOptionsStyles = () => {
  return (
    <Global
      styles={css`
        ${""}
      `}
    />
  );
};

export { CarFilterOptionsStyles };
